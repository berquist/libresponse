---
language: cpp
sudo: false
dist: bionic

addons:
  apt:
    packages:
      - doxygen
      - libarmadillo-dev

compiler:
  - clang
  - gcc

before_script:
  - mkdir -p build
  - cd build

script:
  - cmake ..
  - make
